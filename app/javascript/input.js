function input() {
  const posts = document.querySelectorAll(".post");
  posts.forEach(function (post) {
// ①フォームに文字が入ったらイベントが発火する
    post.addEventListener("input", () => { 
    console.log("integer")
// ②入力した値（数字）を取得する
const integer = new integer(document.getElementById("form"));

// ③取得した数字を使って、利益と手数料を計算する
commission = integer*0.1
profit = integer - commission
// ④計算結果を表示する
const commission
const profit

    });
  });
}
window.addEventListener("load", input);


